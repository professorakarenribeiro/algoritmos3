public class ClasseCliente extends SuperclasseCliente implements InterfaceY {
  // conteúdo da classe concreta
  // deve implementar o corpo para todos os métodos abstratos da Interface Y
  // deve implementar o corpo para todos os métodos abstratos da Superclasse
  // Cliente (se houver)
  // deve implementar o corpo para todos os métodos abstratos da Interface X que
  // não foram implementados ainda pela Superclasse Cliente (se houver)
  // esta classe pode ser instanciada
}